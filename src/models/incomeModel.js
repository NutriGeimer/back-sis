class incomeModel {
    constructor(id, totalIncome) {
        this.id = id; // ID del registro de ingreso
        this.totalIncome = totalIncome; // Ingreso total acumulado
    }
}

export default incomeModel;
